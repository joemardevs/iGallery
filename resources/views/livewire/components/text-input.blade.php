    <input type="{{ $type }}" name="{{ $name }}" id="{{ $id }}"
        class="text-sm my-2 rounded p-2 bg-gray-300 dark:bg-gray-600 focus:outline-none focus:outline-purple-500"
        placeholder="{{ $placeholder }}" autocomplete="{{ $autocomplete }}">
